1. job을 처리중일 때 수정을 금지 시켜야 할까?
   => job의 내용이 바뀌면 작업 내용이 바뀔 수 있다고 가정. 처리중에는 수정 전체 금지
2. 우선 patch 쪽 mutex로 구현
   => TODO 멀티 인스턴스 환경 고려해 분산락 필요
3. job
   => TODO 멀티 인스턴스 환경 고려해 primary server checking logic 필요
   3-1. job 우선 순위
   => 일단 없다고 가정
   3-2. job 의존성
   => 일단 없다고 가정
   3-3. job을 처리하는데 소요되는 시간
   => 1~10초 사이의 랜덤값 생성
   3-4. job 처리 예약 시간
   => 1~20 사이의 랜덤값으로 생성 후 서버 running에 + 해주기 시간으로 계산
   3-5. 완료된 job은 수정 불가능 가정
   3-6. 대기중인 작업, 진행중인 작업 취소 가능
   => TODO processing 중인 job cancel 처리
   3-7. 상태 관련 로직은 별도로 빼는게 좋을듯
   3-8. 대기열 팬딩이 길어지는거 대비해서 버전기록 필요

4. 멀티 뮤텍스락으로 스케쥴러와 api 작업을 구현하던중 스케쥴러의 작업이 길어지면 해당 락이 걸린 job의 대한 변경 가능 여부 조회까지 대기열이 길어지는 이슈 발견
   => lock 상태와 Job 의 status 상태를 이용해서 lock 획득 / 릴리즈 타이밍 조절 필요하다고 판단.
